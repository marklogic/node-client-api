'use strict';
// declareUpdate(); // Note: uncomment if changing the database state

const param1 = external.param1; // instanceof xs.boolean
const param2 = (external.param2 === void 0) ? null : external.param2; // instanceof xs.double?
const param3 = external.param3; // instanceof xs.float+
const param4 = (external.param4 === void 0) ? null : external.param4; // instanceof xs.int*

// TODO:  produce the xs.boolean output from the input variables


true;
